# .dockerignore

# Ignore git repository files
.git
.gitignore

# Ignore local node_modules (they will be installed inside the container)
node_modules

# Ignore the Dockerfile and compose file itself
Dockerfile
docker-compose.yml

# Ignore the data directory, as it will be mounted as a volume
data

# Ignore any local development or OS files
.DS_Store
*.log